# ==========================================
# UPLIFT System Configuration
# ==========================================

# PostgreSQL Database Configuration
DB_USER=postgres
DB_PASSWORD=yuichirokanade
DB_HOST=localhost
DB_PORT=5432
DB_NAME=uplift

# Optional: Full Database Connection URL (overrides individual parameters above if set)
# DATABASE_URL=postgresql://postgres:yuichirokanade@localhost:5432/uplift

# Server Host & Port Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_PORT=5173
